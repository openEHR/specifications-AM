[cols="2,3", frame=none, grid=rows]
|===
2+^h|*C_COMPLEX_OBJECT*

2+a|Constraint on complex objects, i.e. any object that consists of other object constraints.

2+|Inherits: xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS], xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT], xref:{class_c_object}[C_OBJECT], xref:{class_c_defined_object}[C_DEFINED_OBJECT]

2+h|*Constants*

|*Id_code_leader*: `xref:{class_string}[String]{nbsp}={nbsp}"id"` [1..1]
a|String leader of ‘identifier’ codes, i.e. codes used to identify archetype nodes. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*Value_code_leader*: `xref:{class_string}[String]{nbsp}={nbsp}"at"` [1..1]
a|String leader of ‘value’ codes, i.e. codes used to identify codes values, including value set members. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*Value_set_code_leader*: `xref:{class_string}[String]{nbsp}={nbsp}"ac"` [1..1]
a|String leader of ‘value set’ codes, i.e. codes used to identify value sets. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*Specialisation_separator*: `xref:{class_character}[Character]{nbsp}={nbsp}'.'` [1..1]
a|Character used to separate numeric parts of codes belonging to different specialisation levels. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*Code_regex_pattern*: `xref:{class_string}[String]{nbsp}={nbsp}"(0&#124;[1-9][0-9]&#42;)(\.(0&#124;[1-9][0-9]&#42;))&#42;"` [1..1]
a|Regex used to define the legal numeric part of any archetype code. Corresponds to the simple pattern of dotted numbers, as used in typical multi-level numbering schemes. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*Root_code_regex_pattern*: `xref:{class_string}[String]{nbsp}={nbsp}"^id1(\.1)&#42;$"` [1..1]
a|Regex pattern of the root id code of any archetype. Corresponds to codes of the form `id1`, `id1.1`, `id1.1.1` etc.. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*Primitive_node_id*: `xref:{class_string}[String]{nbsp}={nbsp}"id9999"` [1..1]
a|Code id used for `C_PRIMITIVE_OBJECT` nodes on creation. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

2+h|*Attributes*

|*parent*: `xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]` [0..1]
a|Parent node, except in the case of the top of a tree, i.e. root `C_COMPLEX_OBJECT` of an archetype definition. +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

|*soc_parent*: `xref:{class_c_second_order}[C_SECOND_ORDER]` [0..1]
a| +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

|*rm_type_name*: `xref:{class_string}[String]` [1..1]
a|Reference model type that this node corresponds to. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*occurrences*: `xref:{class_multiplicity_interval}[Multiplicity_interval]` [0..1]
a|Occurrences of this object node in the data, under the owning attribute. Upper limit can only be greater than 1 if owning attribute has a cardinality of more than 1.
Only set if it overrides the parent archetype in the case of specialised archetypes, or else the occurrences inferred from the underlying reference model existence and/or cardinality of the containing attribute. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*node_id*: `xref:{class_string}[String]` [1..1]
a|Semantic identifier of this node, used to distinguish sibling nodes. All nodes must have a `_node_id_`; for nodes under a container `C_ATTRIBUTE`, the id must be an id-code must be defined in the archetype terminology. For valid structures, all node ids are id-codes.

For `C_PRIMITIVE_OBJECTs` represented in ADL inline form, this attribute will have the special value `Primitive_node_id`; otherwise it will have the node id read during parsing. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*is_deprecated*: `xref:{class_boolean}[Boolean]` [0..1]
a|True if this node and by implication all sub-nodes are deprecated for use. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*sibling_order*: `xref:{class_sibling_order}[SIBLING_ORDER]` [0..1]
a|Optional indicator of order of this node with respect to another sibling. Only meaningful in a specialised archetype for a `C_OBJECT` within a `C_ATTRIBUTE` with `_is_multiple_ = True`. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*default_value*: `xref:{class_any}[Any]` [0..1]
a|Default value set in a template, and present in an operational template. Generally limited to leaf and near-leaf nodes. +
_Inherited from xref:{class_c_defined_object}[C_DEFINED_OBJECT]_

|*attributes*: `xref:{class_list}[List]<xref:{class_c_attribute}[C_ATTRIBUTE]>` [0..1]
a|List of constraints on attributes of the reference model type represented by this object.

|*attribute_tuples*: `xref:{class_list}[List]<xref:{class_c_attribute_tuple}[C_ATTRIBUTE_TUPLE]>` [0..1]
a|List of attribute tuple constraints under this object constraint, if any.

2+h|*Functions*

|*codes_conformant* ( +
a_child_code: `xref:{class_string}[String][1]`, +
a_parent_code: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|True if `_a_child_code_` conforms to `_a_parent_code_` in the sense of specialisation, i.e. is `_a_child_code_` the same as or more specialised than `_a_parent_code_`? +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*is_adl_code* ( +
a_code: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `Result = is_id_code (a_code) or else is_value_code (a_code) or else is_value_set_code (a_code)` [1..1]
a|True if `_a_code_` is any kind of ADL archetype local code. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*is_id_code* ( +
a_code: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `Result = a_code.starts_with (Id_code_leader)` [1..1]
a|True if `_a_code_` is an 'id' code. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*is_value_code* ( +
a_code: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `Result = a_code.starts_with (Value_code_leader)` [1..1]
a|True if `_a_code_` is an 'at' code, i.e. a code representing a single terminology item. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*is_value_set_code* ( +
a_code: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `Result = a_code.starts_with (Value_set_code_leader)` [1..1]
a|True if `_a_code_` is an 'ac' code, i.e. a code referring to a terminology value set. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*is_redefined_code* ( +
a_code: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|A code has been specialised if there is a non-zero code index anywhere above the last index e.g. 

* `at0.0.1` -> False
* `at1.0.1` -> True +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*code_exists_at_level* ( +
a-code: `xref:{class_string}[String][1]`, +
a_level: `xref:{class_integer}[Integer][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|Is `_a_code_` valid at level `_a_level_` or less, i.e. if we remove its trailing specialised part corresponding to specialisation below `_a_level_`, and then any trailing '.0' pieces, do we end up with a valid code? If so it means that the code corresponds to a real node from `_a_level_` or higher. +
_Inherited from xref:{class_adl_code_definitions}[ADL_CODE_DEFINITIONS]_

|*is_prohibited* (): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `Result = occurrences /= Void and then occurrences.is_prohibited` [1..1]
a|True if this `C_OBJECT` node is prohibited, i.e. if its `_occurrences_` is `0..0`. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*has_path* ( +
a_path: `xref:{class_string}[String][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|True if the relative path `_a_path_` exists at this node. +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

|*path* (): `xref:{class_string}[String]` [1..1]
a|Path of this node relative to root of archetype. +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

|*c_conforms_to* ( +
other: `xref:{class_c_complex_object}[C_COMPLEX_OBJECT][1]`, +
rmcc: `xref:{class_function}[FUNCTION]<xref:{class_boolean}[Boolean],xref:{class_tuple2}[TUPLE2]<xref:{class_string}[String],xref:{class_string}[String]>>[1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|True if constraints represented by this node, ignoring any sub-parts, are narrower or the same as other. 
Typically used during validation of specialised archetype nodes.

.Parameters +
[horizontal]
`_rmcc_`:: Reference Model conformance checker agent (lambda).

|*c_congruent_to* ( +
other: `xref:{class_c_complex_object}[C_COMPLEX_OBJECT][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|True if constraints represented by this node contain no further redefinitions with respect to the node `_other_`, with the exception of `_node_id_` redefinition in `C_OBJECT` nodes. 

Typically used to test if an inherited node locally contains any constraints.

|*is_second_order_constrained* (): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `soc_parent /= Void or else (parent /= Void and then parent.is_second_order_constrained)` [1..1]
a|True if there is a second order constraint such as a tuple constraint on this node. +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

|*is_root* (): `xref:{class_boolean}[Boolean]` [1..1]
a|True if this node is the root of the tree. +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

|*is_leaf* (): `xref:{class_boolean}[Boolean]` [1..1]
a|True if this node is a terminal node in the tree structure, i.e. having no child nodes. +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

|*specialisation_depth* (): `xref:{class_integer}[Integer]` [1..1]
a|Level of specialisation of this archetype node, based on its `_node_id_`. The value 0 corresponds to non-specialised, 1 to first-level specialisation and so on. The level is the same as the number of ‘.’ characters in the node_id code. If `_node_id_` is not set, the return value is -1, signifying that the specialisation level should be determined from the nearest parent `C_OBJECT` node having a node_id. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*effective_occurrences* (): `xref:{class_multiplicity_interval}[Multiplicity_interval]` [1..1]
a|Compute effective occurrences, where no local occurrences constraint set. If the owning `C_ATTRIBUTE._cardinality_` is set, use its upper value, else use RM multiplicity of the owning attribute.

If local `occurrences` not set, always assume 0 as the lower bound. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*occurrences_conforms_to* ( +
other: `xref:{class_c_object}[C_OBJECT][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|True if this node `_occurrences_` conforms to `_other.occurrences_`; `other` is assumed to be in a flat archetype. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

.Parameters +
[horizontal]
`_other_`:: `C_OBJECT` from a flat parent archetype.

|*node_id_conforms_to* ( +
other: `xref:{class_c_object}[C_OBJECT][1]` +
): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `Result = codes_conformant (node_id, other.node_id)` [1..1]
a|True if this node id conforms to `_other.node_id_`, which includes the ids being identical; `_other_` is assumed to be in a flat archetype. +
_Inherited from xref:{class_c_object}[C_OBJECT]_

|*any_allowed* (): `xref:{class_boolean}[Boolean]` +
 +
__Post__: `Result = attributes.is_empty and not is_prohibited` [1..1]
a|True if any value (i.e. instance) of the reference model type would be allowed. Redefined in descendants.

|_(abstract)_ *valid_value* ( +
a_value: `xref:{class_any}[Any][1]` +
): `xref:{class_boolean}[Boolean]` [1..1]
a|True if `_a_value_` is valid with respect to constraint expressed in concrete instance of this type. +
_Inherited from xref:{class_c_defined_object}[C_DEFINED_OBJECT]_

|_(abstract)_ *prototype_value* (): `xref:{class_any}[Any]` [1..1]
a|Generate a prototype value from this constraint object. +
_Inherited from xref:{class_c_defined_object}[C_DEFINED_OBJECT]_

|*has_default_value* (): `xref:{class_boolean}[Boolean]` [1..1]
a|True if there is an assumed value. +
_Inherited from xref:{class_c_defined_object}[C_DEFINED_OBJECT]_

2+h|*Invariants*

2+a|__Post__: `Result = soc_parent /= Void or parent.soc_parent /= Void` +
_Inherited from xref:{class_archetype_constraint}[ARCHETYPE_CONSTRAINT]_

2+a|__Inv_valid_default_value__: `valid_value (default_value)` +
_Inherited from xref:{class_c_defined_object}[C_DEFINED_OBJECT]_

|===
